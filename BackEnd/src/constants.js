
// This module exports default constants used throughout the server's code.

module.exports = {

    default_port: 3000,                 // default server port
    path_uploads: "uploads/",           // path to directory where uploads to server should be stored
    path_stamp: "public/stamp.png",     // path to stamp to be applied on all uploads
    stamp_position: [256,70,0,0]        // size (first 2 elements) and position (second 2 elements) of the stamp

};
